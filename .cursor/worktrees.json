{
  "setup-worktree": [
    "nvm use",
    "yarn",
    "cp $ROOT_WORKTREE_PATH/packages/aegis-server/.env packages/aegis-server/.env || true",
    "cp $ROOT_WORKTREE_PATH/packages/aegis-front/.env packages/aegis-front/.env || true"
  ]
}
