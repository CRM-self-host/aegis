{
  "install": "yarn install",
  "start": "(sudo service docker start || service docker start || true) && bash scripts/setup-dev-env.sh && npx nx database:reset aegis-server",
  "terminals": [
    {
      "name": "Development Server",
      "command": "yarn start"
    }
  ]
}
