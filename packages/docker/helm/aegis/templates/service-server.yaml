{{- if .Values.server.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "aegis.fullname" . }}-server
  namespace: {{ include "aegis.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "aegis.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: server
spec:
  type: {{ .Values.server.service.type | default "ClusterIP" }}
  selector:
    app.kubernetes.io/name: {{ include "aegis.name" . }}
    app.kubernetes.io/component: server
  ports:
    - name: http-tcp
      port: {{ .Values.server.service.port }}
      targetPort: {{ include "aegis.server.containerPort" . }}
      protocol: TCP
{{- end }}
