apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: u84u-db-pvc
  namespace: u84u
spec:
  storageClassName: default
  volumeName: u84u-db-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
