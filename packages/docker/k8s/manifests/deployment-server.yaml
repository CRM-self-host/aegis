apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: u84u-server
  name: u84u-server
  namespace: u84u
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app: u84u-server
  template:
    metadata:
      labels:
        app: u84u-server
    spec:
      volumes:
        - name: u84u-server-data
          persistentVolumeClaim:
            claimName: u84u-server-pvc
        - name: u84u-docker-data
          persistentVolumeClaim:
            claimName: u84u-docker-data-pvc
      containers:
        - name: u84u
          image: u84u/aegis:latest
          imagePullPolicy: Always
          env:
            - name: NODE_PORT
              value: 3000
            - name: SERVER_URL
              value: "https://crm.example.com:443"
            - name: "PG_DATABASE_URL"
              value: "postgres://postgres:postgres@u84u-db.u84u.svc.cluster.local/default"
            - name: "REDIS_URL"
              value: "redis://u84u-redis.u84u.svc.cluster.local:6379"
            - name: SIGN_IN_PREFILLED
              value: "false"
            - name: STORAGE_TYPE
              value: "local"
            - name: "ACCESS_TOKEN_EXPIRES_IN"
              value: "7d"
            - name: "LOGIN_TOKEN_EXPIRES_IN"
              value: "1h"
            - name: APP_SECRET
              valueFrom:
                secretKeyRef:
                  name: tokens
                  key: accessToken
          ports:
            - containerPort: 3000
              name: http-tcp
              protocol: TCP
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1024Mi"
              cpu: "1000m"
          stdin: true
          tty: true
          volumeMounts:
            - mountPath: /app/docker-data
              name: u84u-docker-data
            - mountPath: /app/packages/server/.local-storage
              name: u84u-server-data
      dnsPolicy: ClusterFirst
      restartPolicy: Always
