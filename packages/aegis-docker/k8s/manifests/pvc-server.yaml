apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: u84u-server-pvc
  namespace: u84u
spec:
  storageClassName: default
  volumeName: u84u-server-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
