apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: u84u-db
  name: u84u-db
  namespace: u84u
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app: u84u-db
  template:
    metadata:
      labels:
        app: u84u-db
    spec:
      volumes:
        - name: u84u-db-data
          persistentVolumeClaim:
            claimName: u84u-db-pvc
      containers:
        - name: u84u
          image: u84u/aegis-postgres-spilo:latest
          imagePullPolicy: Always
          env:
            - name: PGUSER_SUPERUSER
              value: "postgres"
            - name: PGPASSWORD_SUPERUSER
              value: "postgres"
            - name: SPILO_PROVIDER
              value: "local"
            - name: ALLOW_NOSSL
              value: "true"
          ports:
            - containerPort: 5432
              name: tcp
              protocol: TCP
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1024Mi"
              cpu: "1000m"
          stdin: true
          tty: true
          volumeMounts:
            - mountPath: /home/postgres/pgdata
              name: u84u-db-data
      dnsPolicy: ClusterFirst
      restartPolicy: Always
